"""
{{ title }} API Models

Generated by Damascus SDK Generator
"""

from dataclasses import dataclass, field
from typing import Dict, List, Any, Optional, Union
from datetime import datetime
from enum import Enum

{% for model_name, model_def in models.items() %}
@dataclass
class {{ model_name }}:
    """{{ model_def.description or model_name + " model" }}"""
    {% for field_name, field_def in model_def.required_fields %}
    {{ field_name }}: {{ field_def.type }}
    {% endfor %}
    {% for field_name, field_def in model_def.optional_fields %}
    {{ field_name }}: Optional[{{ field_def.type }}] = None
    {% endfor %}
{% endfor %} 